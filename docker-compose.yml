services:
  monitor:
    build: .
    ports:
      - "5001:5000"
    environment:
      - HOST=0.0.0.0
      - DATA_DIR=/app/data
    env_file:
      - .env
    volumes:
      - ./data:/app/data
    restart: unless-stopped
